services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: omnifilter
      POSTGRES_PASSWORD: password
      POSTGRES_DB: omnifilter
    ports:
      - "5432:5432"
    volumes:
      - omnifilter_postgres:/var/lib/postgresql/data
volumes:
  omnifilter_postgres:
