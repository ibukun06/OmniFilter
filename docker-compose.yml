version: "3.9"

services:

  postgres:
    image: postgres:16

    environment:
      POSTGRES_USER: omnifilter
      POSTGRES_PASSWORD: password
      POSTGRES_DB: omnifilter

    ports:
      - "5432:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
