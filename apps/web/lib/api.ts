const base=process.env.NEXT_PUBLIC_API_URL||'http://localhost:3001/api'; export function token(){return typeof window==='undefined'?null:localStorage.getItem('omnifilter_token')} export async function api(path:string,options:RequestInit={}){const headers:any={'Content-Type':'application/json',...(options.headers||{})}; const t=token(); if(t) headers.Authorization=`Bearer ${t}`; const r=await fetch(`${base}${path}`,{...options,headers}); const data=await r.json().catch(()=>({})); if(!r.ok) throw new Error(data.message||'Request failed'); return data;}
